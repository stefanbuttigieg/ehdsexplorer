// With registerType: "autoUpdate" + skipWaiting + clientsClaim in vite.config.ts,
// the service worker updates automatically. No manual registration needed here.
const ReloadPrompt = () => null;

export default ReloadPrompt;
